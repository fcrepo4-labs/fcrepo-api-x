
description = ''
dependencies {
  compile project(':fcrepo-api-x-model')
  compile project(':fcrepo-api-x-registry')
    compile(group: 'org.apache.jena', name: 'jena-osgi', version:'3.1.1') {
exclude(module: 'org.osgi.core')
    }
    compile group: 'com.github.andrewoma.dexx', name: 'collection', version:'0.6'
    compile group: 'org.slf4j', name: 'jcl-over-slf4j', version:'1.7.13'
    compile group: 'org.apache.servicemix.bundles', name: 'org.apache.servicemix.bundles.xmlresolver', version:'1.2_5'
    compile(group: 'org.apache.servicemix.bundles', name: 'org.apache.servicemix.bundles.xerces', version:'2.11.0_1') {
exclude(module: 'xml-apis')
    }
    testCompile group: 'junit', name: 'junit', version:'4.12'
    testCompile group: 'org.mockito', name: 'mockito-all', version:'1.10.19'
    testCompile group: 'org.apache.jena', name: 'jena-core', version:'3.1.1'
  testCompile project(':fcrepo-api-x-test')
    testCompile group: 'org.slf4j', name: 'slf4j-simple', version:'1.7.13'
    compile(group: 'org.osgi', name: 'org.osgi.service.component.annotations', version:'1.3.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.slf4j', name: 'slf4j-api', version:'1.7.13') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
